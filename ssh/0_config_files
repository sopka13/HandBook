// СИСТЕМНЫЕ файлы конфигурации SSH:

// настройки сервера SSH. Определяет параметры, такие как порт прослушивания,
// ограничения доступа, аутентификацию и шифрование
/etc/ssh/sshd_config

// глобальные настройки клиента SSH. Определяет параметры, такие как адрес
// сервера, порт, аутентификацию и шифрование
/etc/ssh/ssh_config

// публичные ключи удаленных серверов, с которыми ранее устанавливалось
// соединение
/etc/ssh/ssh_known_hosts

// команды и настройки, выполняемые при каждом подключении к удаленному серверу
/etc/ssh/sshrc

// приватные ключи хоста SSH, используемые сервером SSH для аутентификации
/etc/ssh/ssh_host_*_key

// ПОЛЬЗОВАТЕЛЬСКИЕ файлы конфигурации SSH (хранятся в домашнем каталоге):

// пользовательские настройки клиента SSH. Определяет параметры для конкретных
// удаленных хостов или групп хостов
~/.ssh/config

// записи публичных ключей удаленных серверов, с которыми устанавливалось
// соединение
~/.ssh/known_hosts

// приватные ключи аутентификации пользователя
~/.ssh/id_rsa, ~/.ssh/id_dsa, ~/.ssh/id_ecdsa, ~/.ssh/id_ed25519

// публичные ключи аутентификации пользователя
~/.ssh/id_*.pub

// публичные ключи, разрешенные для аутентификации на удаленных серверах
~/.ssh/authorized_keys