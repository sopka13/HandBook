// Литералы
	a, b, c ... - буквы;
	-			- дефис;

// Метасимволы
	^	- циркумфлекс - начало строки;
		пример: ^cat => найдет совпадения когда cat будет находится в начале строки;
	$	- доллар - конец строки;
		пример: cat$ => найдет совпадения когда cat будет находится в конце строки;
	.	- любой символ;
		пример: 19.02.132 => найдет 19/02/132, 19 02 132, 198029132 ...;
	|	- вертикальный слэш - или, альтернатива;
		пример:
		Bob|Robert => найдет Bob или Robert;
		Jeffrey|Jeffery или Jeff(rey|ery) или Jeff(re|er)y дадут один и тот же результат;
	\<	- обратный слэш + знак меньше - начало слова;
		пример: \<cat => найдет cat, catet, cat1234;
	\>	- обратный слэш + знак больше - конец слова;
		пример: \<cat\> => найдет cat и все;
	()	- круглые скобки (инвертирование внутри не работает)
		пример: \<(1,000,0001|million |thousand thou)\> => найдет одно из значений заключенных в скобки;
	?	- вопросительный знак (квантификатор) - необязательный элемент (один или ноль);
		пример:
		colou?r => найдет color и colour;
		4(th)? => найдет 4th и 4;
	+	- плюс (квантификатор) - один или несколько экземпляров предшествующего элемента,
		(один или больше);
	*	- звездочка (квантификатор) - любое количество экземпляров предшествующего элемента,
		в том числе и ноль (ноль или больше);
	{}	- интервальный квантификатор - интервал количества экземпляров ...{min, max};
		пример:
		[a-z]{1,5} => найдет любые строчные символы в 1 или 5ти экземплярах;
		...{0, 1} эквивалентно метасимволу ?;
	\1	- обратные ссылки - ссылка на группировку ();
		пример:
		(the) +\1 => найдет the the, the theory и т.д.;
		\<([A-Za-z]+) +\1\> => найдет повторяющиеся слова между которыми 1 или больше пробелов;
		\<([A-Za-z]+) +and +\1\> => найдет more and more и подобные;
		([A-Za-z]+)_([0-9]+) +\1\2 => найдет повторяющиеся последовательности типа http_12 http_12 и пр.;
	\	- экранирование (не работает в символьных классах *1)(исключения \<, \>, \1, \2);
		пример:
		ega.att.com => найдет ega.att.com, megavattfcom ... ;
		ega\.att\.com => найдет  ega.att.com, mega.att.com, mega.att.coms ... ;

// Символьные классы []
	[]	- ищет совпадение с одим из символов в таких скобках
		пример:
		c[ae]t => найдет совпадения cat и cet;
		sep[ae]r[ae]te => найдет совпадения saparate, seperate, seperate, separete;
		[Ss]mith => найдет совпадения smith, Smith.
	-	- обозначает интервал (только внутри символьного класса и между литералами []);
		пример: H[0-3] => найдет совпадения H1, H2, H3;
	^	- инвертированные символы (!!! только если он стоит сразу после открывающейся скобки
		иначе интерпретируется как простой символ)
		пример:
		c[^ae]t => найдет совпадения все кроме cat и cet;
		"[^"]*" => найдет все выражения в кавычках, [^"] - значит любой символ кроме кавычек;
	.	- литерал;
	|	- литерал;
	\	- литерал *1;

***
1. Зависит от реализации языка;

// Примеры
1. [A-Fa-f0-9]	- обработка шеснацетиричных чисел;
2. "[^"]*"		- найдет все выражения в кавычках, [^"] - значит любой символ кроме кавычек;